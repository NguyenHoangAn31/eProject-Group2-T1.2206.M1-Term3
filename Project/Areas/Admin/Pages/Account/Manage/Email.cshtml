@page
@model EmailModel
@{
    ViewData["Title"] = "Manage Email";
    ViewData["ActivePage"] = ManageNavPages.Email;
    Layout = "_Layout";
}

<h3 class="mt-3">@ViewData["Title"]</h3>
<partial name="_StatusMessage" for="StatusMessage" />
<div >
    <div>
        <form class="needs-validation" novalidate id="email-form" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            @if (Model.IsEmailConfirmed)
            {
                @*<div class="form-floating input-group">
                    <input asp-for="Email" class="form-control" disabled />
                        <div class="input-group-append">
                            <span class="h-100 input-group-text text-success font-weight-bold">✓</span>
                        </div>
                    <label asp-for="Email" class="form-label"></label>
                </div>*@

                <div class="row mb-3">
                    <label asp-for="Email" class="col-md-3 col-lg-2 col-form-label"></label>
                    <div class="col-md-9 col-lg-10">
                        <input asp-for="Email" class="form-control" disabled>
                    </div>
                </div>
            }


            
            else
            {
                @*<div class="form-floating">
                    <input asp-for="Email" class="form-control" disabled />
                    <label asp-for="Email" class="form-label"></label>
                    <button id="email-verification" type="submit" asp-page-handler="SendVerificationEmail" class="btn btn-link">Send verification email</button>
                </div>*@

                <div class="row mb-3">
                    <label asp-for="Email" class="col-md-3 col-lg-2 col-form-label"></label>
                    <div class="col-md-9 col-lg-10">
                        <input asp-for="Email" class="form-control" disabled>
                        <button id="email-verification" type="submit" asp-page-handler="SendVerificationEmail" class="btn btn-link">Send verification email</button>
                    </div>
                </div>

            }
            @*<div class="form-floating">
                <input asp-for="Input.NewEmail" class="form-control" autocomplete="email" aria-required="true" />
                <label asp-for="Input.NewEmail" class="form-label"></label>
                <span asp-validation-for="Input.NewEmail" class="text-danger"></span>
            </div>*@
            <div class="row mb-3">
                <label asp-for="Input.NewEmail" class="col-md-3 col-lg-2 col-form-label"></label>
                <div class="col-md-9 col-lg-10">
                    <input asp-for="Input.NewEmail" class="form-control" required>
                </div>
            </div>
            <div class="text-center">
                <button type="submit" asp-page-handler="ChangeEmail" class="btn btn-primary">Save Changes</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
